# Samples

This directory is for captured outputs from real systems.

Recommended naming:

```text
samples/
  x86_64-nvidia-h100/
    snapshot.json
    notes.md
  x86_64-amd-mi300x/
    snapshot.json
    notes.md
  arm64-nvidia/
    snapshot.json
    notes.md
```

The first committed example here is synthetic and fixture-backed so the repository has a stable schema example even before large real-hardware captures land.

